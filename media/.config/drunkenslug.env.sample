# Sample environment for DrunkenSlug + Prowlarr setup
#
# Copy this file to media/.config/drunkenslug.env and fill in the real values.
#
# For full API reference and quota rules, see: ../prowlarr/DRUNKENSLUG.md
# For safe setup and usage, see: https://trash-guides.info/Prowlarr/prowlarr-setup-limited-api/

# DrunkenSlug indexer
# The API key provided by DrunkenSlug. Do NOT commit the real key to git.
DRUNKENSLUG_API=
# The base URL for the DrunkenSlug Newznab-compatible API (official)
# Use the Newznab endpoint when configuring indexers, for example:
# https://drunkenslug.com/api
DRUNKENSLUG_URL=https://drunkenslug.com/api

# Prowlarr connection details
# Prowlarr base URL (adjust if it's running on a different host/port)
PROWLARR_URL=http://localhost:9696
# Your Prowlarr API key (can be found under Settings -> General -> Security)
PROWLARR_API=

# Optional: limits you want to enforce (for documentation only)
DS_API_HITS_PER_DAY=25
DS_DOWNLOADS_PER_DAY=5
