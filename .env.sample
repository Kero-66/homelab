# Sample environment variables for the media stack
# Copy to .env and edit as needed

# Common
PUID=1000
PGID=1000
TZ=UTC
CONFIG_DIR=.       # where per-service config folders are stored
DATA_DIR=/data     # where media files live

# Ports for new services (override to avoid conflicts)
UBOOQUIITY_PORT=2202
MYLAR3_PORT=8090

# Static IP fallbacks on the `servarrnetwork` (optional)
IP_UBOOQUIITY=172.39.0.18
IP_MY3=172.39.0.19

# Leave other existing service vars in your existing .env (sonarr/radarr/etc.)
