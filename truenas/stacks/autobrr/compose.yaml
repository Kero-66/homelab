services:
  autobrr:
    # Deliberately pinned — a prior upgrade attempt past this version caused issues.
    # Current upstream is v1.84.0+ as of 2026-08-15. Test carefully before bumping (see ai/todo.md).
    image: ghcr.io/autobrr/autobrr:v1.79.0
    container_name: autobrr
    restart: unless-stopped
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Australia/Brisbane
    volumes:
      - /mnt/Fast/docker/autobrr/config:/config
    ports:
      - "7474:7474"
    networks:
      - arr-stack_default
      - downloaders_default
    labels:
      - homepage.group=Downloads
      - homepage.name=autobrr
      - homepage.icon=autobrr
      - homepage.href=http://autobrr.home
      - homepage.description=Release automation

networks:
  arr-stack_default:
    external: true
  downloaders_default:
    external: true
