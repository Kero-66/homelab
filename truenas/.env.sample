# TrueNAS Scale CE Configuration
# Copy to .env and fill in your values: cp .env.sample .env
#
# Reference: https://api.truenas.com/v25.10/

# --- Connection ---
# TrueNAS IP address (set static IP in TrueNAS console first)
TRUENAS_IP=
# SSH user (default admin user created during TrueNAS install)
TRUENAS_USER=admin

# --- Timezone ---
TZ=Australia/Brisbane

# --- Pool Names ---
# NVMe mirror pool (~1TB usable) - fast storage for apps, databases, containers
POOL_FAST=fast
# HDD mirror pool (~8TB usable) - bulk storage for media, backups, archives
POOL_BULK=bulk

# --- Dataset Layout ---
# Datasets are created under each pool as: poolname/datasetname
#
# fast pool datasets:
#   fast/apps        - Container/app configuration and data
#   fast/databases   - PostgreSQL, Redis, etc.
#   fast/docker      - Docker dataset for TrueNAS apps/containers
#
# bulk pool datasets:
#   bulk/media       - Movies, TV, music (Jellyfin library)
#   bulk/photos      - Photo library (Immich originals)
#   bulk/cloud-sync  - Google Drive, Dropbox mirrors
#   bulk/backups     - Backup target for other machines
#   bulk/downloads   - Download staging area

# --- User/Group IDs ---
# Match these to your container PUID/PGID for permission consistency
PUID=1000
PGID=1000
# TrueNAS local user for SMB shares and file ownership
NAS_USER=nasuser
