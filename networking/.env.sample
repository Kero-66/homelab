# Copy to .env and edit values for your environment
PUID=1000
PGID=1000
TZ=UTC
# Config directory for persistent service data (relative to this folder). For this stack use the `networking/.config` folder
CONFIG_DIR=.config

# Ports (change only if conflict)
ADGUARD_DNS_PORT=53
ADGUARD_HTTP_PORT=80
ADGUARD_HTTPS_PORT=443

# Docker restart policy
RESTART_POLICY=unless-stopped

# Caddy (reverse proxy)
# Set CADDY_EMAIL to your contact address for Let's Encrypt and add your domain(s)
CADDY_EMAIL=your-email@example.com
# Space/comma separated domains used for your proxy config (one domain per site in the Caddyfile)
CADDY_DOMAINS=example.com
CADDY_HTTP_PORT=80
CADDY_HTTPS_PORT=443

# Data and network defaults (media-like)
# Base data directory for persistent mounts (change to your storage location)
DATA_DIR=/mnt/wd_media/homelab-data

# Docker network for this stack (choose a free subnet, avoid conflicts with other stacks)
NETWORK_SUBNET=172.30.0.0/24
# Static IPs for services on the networking stack (optional)
IP_ADGUARD=172.30.0.3
IP_CADDY=172.30.0.4

# Notes:
# - Ensure NETWORK_SUBNET doesn't conflict with other project subnets (e.g., media uses 172.39.0.0/24)
# - If you prefer to assign IPs dynamically, leave the IP_* variables unset
