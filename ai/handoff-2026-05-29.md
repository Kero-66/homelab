# Handoff — 2026-05-29 (session 2)

## Repo
`/Users/kieran/repos/homelab` (branch: main)

## What Happened This Session

### 1. Mempalace Recall Root Cause
- **Problem**: mempalace storing memories but never recalling — agent repeating same mistakes
- **Root cause**: `UserPromptSubmit` and `SessionStart` hooks NOT registered in settings. mempalace design is passive save-only at hook level; recall is agent-driven via `mempalace wake-up` + MCP tools.
- **Fix applied**: Added `mempalace wake-up` as step 5 in `.claude/CLAUDE.md` start-of-session checklist

Compared to `zircote/mnemonic` which forces recall via `UserPromptSubmit` hook injecting `additionalContext` before every response. If recall issues persist, mnemonic is the architectural alternative.

### 2. Palace Health — Corrupt HNSW Segments
- **Root cause**: Two `mempalace-mcp` processes running simultaneously (one from Saturday, one from today). Both write to same sqlite but each has own in-memory HNSW — flush on exit overwrites the other.
- **Fix applied**: Killed stale process. Single MCP server now running.
- **Repair run**: `mempalace repair --mode from-sqlite --archive-existing --backup --yes`
  - Rebuilt 8,656 rows (4,675 drawers + 3,981 closets)
  - Corrupt archive at: `~/.mempalace/palace.pre-rebuild-20260529-140944`
- **Post-repair gotcha**: sqlite left as 644 (read-only) after repair
  - Fix: `chmod 664 ~/.mempalace/palace/chroma.sqlite3` then `/mcp` in Claude Code to reconnect

### 3. Agent Instructions Migration
**Problem**: Three overlapping instruction systems from Copilot→Claude Code migration.

**Critical fix**: `ai/settings.json` was NOT loaded by Claude Code (wrong path).
- Claude Code loads: `~/.claude/settings.json` (global) + `.claude/settings.json` (project)
- `ai/settings.json` was invisible — `PreToolUse` security hook, `effortLevel:low`, `verbose:true` all inactive
- **Fix**: moved `ai/settings.json` → `.claude/settings.json` — now active

**Archived** to `archive/legacy-agent-instructions/`:
- `.github/copilot-instructions.md`, `memory.md`, `homelab_env.md`
- `ai/ai_behaviour.md`, `improve_copilot_instructions.md`, `copilot-instructions-template.md`
- `.claude/backup-2026-03-29/`

**Content merged into `.claude/CLAUDE.md`** (new sections):
- Service Migrations — research before action, TrueNAS path conventions
- Recyclarr Rules — profiles, anime audio -10000 penalty, naming
- Code Standards — bash `set -euo pipefail`, shellcheck, commit format

**Clean layout now**:
```
.claude/          Claude Code config (CLAUDE.md, settings.json, memory/, rules/)
ai/               Working files (SESSION_NOTES, todo, PATTERNS, hooks/, handoffs)
.github/          GitHub-specific only (TROUBLESHOOTING.md, GPU_PASSTHROUGH_CONTEXT.md)
archive/          Retired files
```

### 4. Mempalace Noise — Interrupted
- `homelab/general` room: 2,575 drawers including raw API JSON dumps
- Noise sources: `scripts/sonarr_episodes_*.json`, `scripts/sonarr_search_episode_*.json`, `scripts/prow_search_*.json`
- **Problem**: mempalace only respects `.gitignore` for mine exclusions — no separate `.mempalaceignore`
- **Session interrupted** before resolving: user wants to exclude from mining WITHOUT affecting git tracking
- **Options to investigate next session**:
  - `.git/info/exclude` — local git excludes, not committed to repo, mempalace may respect these
  - Global git ignore at `~/.config/git/ignore` — affects local machine only
  - Whether mempalace respects patterns in `~/.mempalace/` config

## Current Palace State
```
Total drawers: 4,839
Wings: wing_homelab(71) + sessions(2,208) + homelab(2,560)
KG: 25 entities, 16 triples (sparse)
Tunnels: 3 (architecture, technical, problems bridge sessions↔wing_homelab)
MCP: single server running
```

## Open Work

### High Priority
1. **Mempalace noise exclusion** — exclude `archive/` and `scripts/*.json` from mining without gitignoring. Investigate `.git/info/exclude` and `~/.config/git/ignore` as local-only solutions.
2. **KG backfill** — 16 triples for 4,839 drawers. `mempalace_kg_query` barely works. Add homelab-specific triples: service→host, service→port, stack→services.

### Medium Priority
3. **Periodic repair reminder** — add to `ai/todo.md`: "if search feels degraded, run `mempalace repair --mode from-sqlite --archive-existing --backup --yes`"
4. **`scripts/` JSON cleanup** — are `sonarr_episodes_*.json`, `prow_search_*.json` still needed? If not, delete them.

## Maintenance Commands (verified)
```bash
# Check for stale MCP servers (kill beyond current session)
ps aux | grep mempalace-mcp | grep -v grep

# Repair corrupt HNSW
mempalace repair --mode from-sqlite --archive-existing --backup --yes
chmod 664 ~/.mempalace/palace/chroma.sqlite3
# Then /mcp in Claude Code to reconnect

# Session start (per CLAUDE.md step 5)
mempalace wake-up
```

## Suggested Skills
- `/mempalace:status` — verify palace health at session start
- `/mempalace:search` — find past decisions before working on a service

## Key Files
| File | Purpose |
|------|---------|
| `.claude/CLAUDE.md` | Primary instructions — updated this session |
| `.claude/settings.json` | Project settings — moved from ai/, now active |
| `ai/PATTERNS.md` | Verified SSH/Infisical/API commands — read before any command |
| `archive/legacy-agent-instructions/` | All retired Copilot-era instruction files |
