# Handoff: Sonarr/Radarr Gap-Fixing Continuation, Memory Cleanup, Maintainerr Fix

**Date**: 2026-08-20
**Repo**: `/Users/kieran/repos/homelab`
**Commits this session**: `8e770c5` (docs/media audit + memory consolidation), pushed to `main`. See its message for a full summary — not duplicated here.

---

## Where the Sonarr/Radarr gap audit actually stands

**Read `media/docs/SONARR_STRUCTURAL_AUDIT.md` in full before continuing** — it's the durable reference, updated extensively this session. Do not re-derive what's already there.

**Do NOT assume this work is "done."** A commit message earlier in this session claimed the audit was "complete" and the user corrected that immediately — there is real, identified, un-actioned work remaining (see below). Confirm current state via the API before assuming anything in the doc is still accurate; it goes stale fast.

### Closed out this session (verified, don't re-check)
- 11 series had **zero real numbered-season gaps** — entire "gap" status was Season 0 specials, already resolved: Attack on Titan, Gundam Wing, Gundam 0083, .hack, Macross 7/Frontier/Macross/Zero/Plus, Tekkaman Blade, Gurren Lagann.
- `.hack` series (55) fully assessed at the Season 0 level — see the doc's dedicated table. Two duplicates unmonitored (`.hack//GIFT`, `.hack//Quantum`'s 3 core episodes), several real gaps searched, several bonus/non-core items correctly deprioritized.
- **Reverse duplicate-series pattern discovered**: a Sonarr *special* can duplicate a whole separate *standalone series* (not just a Radarr movie, not just a season-within-a-series — the inverse of the already-known VOTOMS/Tekkaman Blade II pattern). Confirmed instances: `.hack//Legend of the Twilight`'s "Offline Meeting Special" = `.hack`'s "Let's Meet Offline" special = a Radarr entry (all missing, don't chase 3x). Gurren Lagann's "Parallel Works" specials duplicated a real standalone series that didn't exist in Sonarr yet — added it (series id 147), unmonitored the 7 parent-series duplicates.
- **Two brand-new, 100%-missing series added by the user mid-session**: `.hack//Roots` (145, 0/27) and `.hack//Legend of the Twilight` (146, 0/13) — both season-searched, **zero results on both**.
- A critical **methodology bug fixed**: the whole movie-audit workflow only ever checked Season 0 items with `runtime>=60min`, silently excluding every short special library-wide. Fixed in the doc's workflow section (step 1/2b) — runtime is now documented as a triage signal, not a filter for what to check at all.

### Still genuinely open — pick up here
1. **Not yet triaged**: Gundam Wing (29) "30th Anniversary Video" + "Introducing Gundam Wing from ZERO"; Macross 7 (67) "Let's Bomber". Short specials, likely non-core promo, but not confirmed with a source yet.
2. **`.hack` "Online Jack" 01-09** (9 shorts, 2-4min each) — confirmed to be real distinct franchise content but format/significance never pinned down. No TVDB standalone-series match found. Left monitored, undecided.
3. **Zero-result searches, not yet re-tried**: Robotech (49 eps, entire S2+S3 — matches a known 2026-08-07 content-scarcity diagnosis, probably a real dead end not a scoping bug), Zoids: Chaotic Century (29 eps, S1E6-34), Gasaraki (1 ep, previously-confirmed dead magnet), The Big O (2 eps), Macross II (1 ep), `.hack//Roots`, `.hack//Legend of the Twilight`, Gurren Lagann Parallel Works (0/15).
4. **The reverse-duplicate-pattern sweep is NOT done library-wide** — only checked for `.hack` and Gurren Lagann so far. Given how many series exist (150+), a full sweep grouping by franchise-name stem hasn't been run. Worth doing before assuming any other franchise's specials are "just bonus content."
5. **Maison Ikkoku** (144) — episodes 1-2 and 22-26 of Season 1 still not searched/found (episodes 3-6 landed via a re-grab after the first pack stalled dead).

### Prowlarr indexer scoping — do this before more searches
`ai/todo.md` item #94: an unusually high number of searches this session came back with **zero results across all indexers** (see list above). Before continuing to grind through more individual searches, check whether Prowlarr's indexers have their **sync categories** (not "categories" — confirmed correct terminology by the user) scoped too restrictively, excluding anime/OVA/older-TV categories that would actually carry these releases. This could explain most of the open items in section 3 above in one fix.

There's also a documented escape hatch in `ai/PATTERNS.md` ("Escape hatch: searching directly via Prowlarr when Sonarr/Radarr's own search finds nothing") for testing individual indexers with free-text queries before touching the sync-category config — useful for confirming whether it's a scoping issue or genuine unavailability. Also try Sonarr's `SeriesSearch` (whole-series scope, broader than `SeasonSearch`) on the zero-result series before reaching for the Prowlarr escape hatch — it stays in the normal automated grab path.

---

## Maintainerr — resolved this session

"Tags from Maintainerr aren't applying to movies" — root cause found and fixed: a transient Jellyfin connection failure at Maintainerr container startup (`Invalid API key` in logs, but the key itself tested valid — looks like a startup race, Jellyfin not ready yet) left the "Remove watched movies not tagged retain" rule's collection with `mediaServerId: null`, meaning it never linked to Jellyfin and never actually ran (`handledMediaAmount: 0`, `lastDurationInSeconds: 0`). Verified after a restart that `mediaServerId` is now populated and the rule has executed (`lastDurationInSeconds: 12`). No further action needed unless it recurs — if it does, the fix is restarting Maintainerr via `docker compose` (it's Dockhand-managed per `ai/todo.md` #77), not a raw `docker restart` (see `feedback_dockhand_apps_no_raw_docker_commands.md`).

---

## Memory system housekeeping — important, don't repeat

This session repeated a mistake from 2026-08-15: memory got written to the wrong location (`~/.claude/projects/-Users-kieran-repos-homelab/memory/`, the harness's default auto-memory path) instead of the repo's own `.claude/memory/`, which CLAUDE.md explicitly requires. **Fixed this session** — full audit and reconciliation done:

- Repo `.claude/memory/` now has everything homelab-specific (10 files migrated in from global that had never been there, 2 files where global had strictly newer content synced in).
- Global `~/.claude/projects/.../memory/` was cleaned down to only 5 genuinely cross-project behavioral files, with a redirect note added to its `MEMORY.md` explaining the split.
- **Discipline going forward**: write memory directly to `/Users/kieran/repos/homelab/.claude/memory/` from the start of a session — do not let the harness's default instinct write to the global path first.

Also fixed a stale doc bug that caused a repeated failed lookup this session: `ai/PATTERNS.md` had two contradictory entries for where `JELLYFIN_API_KEY` lives in Infisical (one said root path `/`, wrong; the correct one, `/media`, was already documented elsewhere in the same file and in `MEMORY.md`). The stale entry is now deleted.

**Incident to be aware of**: a real fragment of the live Jellyfin API key was briefly printed into this session's transcript while debugging (justified at the time as "just confirming a match" — it wasn't safe). New memory `feedback_never_print_secret_fragments.md` documents this. **Consider rotating the Jellyfin API key** given the exposure, if not already done.

---

## Suggested skills for next session

- **No specific investigation skill strongly indicated** for the Sonarr/Radarr work — it's continuation of an established manual API-driven workflow (see `ai/PATTERNS.md`), same as prior sessions.
- If the Prowlarr sync-category review turns into a bigger investigation (e.g. indexer behavior is inconsistent or hard to explain), **`diagnose`** fits well — reproduce→minimize→hypothesize→fix→regression-test.
- If picking up the reverse-duplicate-pattern sweep across the full library (150+ series), consider whether it's large enough to warrant a dedicated plan via **`Plan`** before diving in — it's a big, structured task with a clear methodology already documented, but covers a lot of ground.
