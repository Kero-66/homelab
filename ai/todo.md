# AI / Project TODO

This file records suggestions, unanswered questions, and follow-ups the AI should append when the user does not answer.

Format: id | title | description | created_by | created_at | status | related_files

3 | allow_copilot_update | Permission to update `.github/copilot-instructions.md` to record AI behaviour rules (unanswered questions auto-appended to `ai/todo.md`)? | AI | 2025-12-16 | open | .github/copilot-instructions.md, ai/improve_copilot_instructions.md

5 | ci_secret_scan | Add CI/pre-commit secret-scan and ensure `.sample` files exist for all tracked configs | AI | 2025-12-16 | open | .github/, .gitignore

---

Notes for AI behaviour (to be mirrored into `.github/copilot-instructions.md` and `ai/improve_copilot_instructions.md`):

- When a user question remains unanswered after a reasonable clarification attempt, append the question to `ai/todo.md` with status `open` and continue with safe non-destructive work.
- Always record external sources used into `ai/reference.md` when the AI references external documentation.
- Do not modify or commit secrets; create `.sample` files instead.

11 | better_stack_review | Re-evaluate Better Stack for external fail-safe monitoring (deferred: cloud-only) | GitHub Copilot | 2026-01-08 | open | ai/ideas.md
15 | bazarr_language_config | Configure Bazarr wanted languages (English/Japanese) to ensure multiple subtitles are downloaded | GitHub Copilot | 2026-01-10 | open | media/bazarr/config/config.yaml

---

## Repository Security & Architecture Review (2026-01-15)

16 | fix_plaintext_passwords | Remove plaintext password from monitoring/.env and implement proper credential management | AI | 2026-01-15 | open | monitoring/.env, networking/.config/.credentials.sample

17 | add_security_headers | Implement security headers in Caddy reverse proxy configuration (CSP, HSTS, X-Frame-Options) | AI | 2026-01-15 | open | networking/.config/caddy/Caddyfile

18 | create_root_orchestration | Add root-level docker-compose.yml to orchestrate all stacks with proper service dependencies | AI | 2026-01-15 | open | docker-compose.yaml (root), */compose.yaml

19 | standardize_env_vars | Standardize environment variable naming conventions across all stacks (networking, monitoring, automations) | AI | 2026-01-15 | open | */.env, */.env.sample

20 | harden_docker_networking | Review and reduce network_mode: host usage, evaluate privileged containers, implement proper network isolation | AI | 2026-01-15 | open | proxy/compose.yaml, surveillance/compose.yaml, automations/compose.yml

---

## TrueNAS Migration Session (2026-02-11) - CRITICAL ISSUES

23 | cleanup_workstation_services | Optional: Stop and cleanup remaining containers on workstation (192.168.20.66) and verify cold spare status | AI | 2026-02-11 | open | /mnt/library/repos/homelab/media/

---

## TrueNAS Jellyfin Migration (2026-02-11)

46 | truenas_vpn_integration | DEFERRED: VPN (gluetun) integration for downloaders documented but not deployed. Can be added later if needed. Compose includes commented template | GitHub Copilot | 2026-02-11 | open | truenas/stacks/downloaders/compose.yaml

51 | truenas_homepage_remote_access | Configure Homepage to access TrueNAS containers (192.168.20.22) instead of localhost. Caddy reverse proxy or direct IP configuration needed. | Claude | 2026-02-12 | open | apps/homepage/, networking/

53 | claude_memory_review | Review and consolidate .claude/memory files to properly reference .github and ai/ folder content. Ensure consistency and remove duplication. | Claude | 2026-02-12 | open | ~/.claude/projects/-mnt-library-repos-homelab/memory/, .github/, ai/

54 | truenas_docs_review | Review truenas/ directory structure and documentation for correctness, clarity, and organization. Consolidate or restructure as needed. | Claude | 2026-02-12 | open | truenas/

57 | standardize_kero66_password | Update kero66 password to be consistent across all services (TrueNAS, Infisical, AdGuard, etc.) for password manager storage. Currently using different passwords. | User | 2026-02-12 | open | Various services

---

## Architecture & Improvements (2026-01-15)

51 | add_centralized_logging | Implement centralized logging solution (ELK stack or similar) for all homelab services | AI | 2026-01-15 | open | monitoring/, */compose.yaml

52 | implement_health_checks | Add comprehensive health checks to all services missing them, create monitoring dashboard | AI | 2026-01-15 | open | */compose.yaml, monitoring/

53 | create_backup_verification | Verify backup scripts cover all persistent data, add automated backup testing and off-site strategy | AI | 2026-01-15 | open | backup_*.sh, */README.md

54 | add_deployment_automation | Create deployment scripts with rollback capability and configuration drift detection | AI | 2026-01-15 | open | scripts/, */README.md

55 | document_service_dependencies | Create comprehensive documentation of service interdependencies, startup order, and network topology | AI | 2026-01-15 | open | docs/, README.md

56 | fix_hardcoded_ips | Replace hardcoded IPs with environment variables or service discovery (automations, proxy configs) | AI | 2026-01-15 | open | automations/compose.yml, proxy/compose.yaml

57 | bazarr_sync | Configure Bazarr to prioritize subtitle languages and providers, aligning with Anime/Standard logic | AI | 2026-01-15 | open | media/bazarr/config/config.yaml

58 | fileflows_optimization | Review and consolidate FileFlows sandbox scripts and optimize for hardware acceleration | AI | 2026-01-15 | open | media/fileflows/

59 | prowlarr_tagging | Define Prowlarr indexer tags to align with Recyclarr's Anime/Standard profiles | AI | 2026-01-15 | open | prowlarr/
60 | caddy_optional_proxies | Add Caddy routes for optional media services (FileFlows, Cleanuparr) so they are reachable whenever enabled instead of being blocked by the current proxy | GitHub Copilot | 2026-01-15 | open | networking/.config/caddy/Caddyfile, media/compose.yaml
61 | prowlarr_stats_visualization | Review and set up Exportarr + Prometheus + Grafana for Prowlarr indexer stats visualization | User | 2026-01-18 | open | monitoring/
62 | organizr_dashboard | Review and potentially set up Organizr for *arr apps dashboard | User | 2026-01-18 | open | apps/
63 | monitoring_observability_logs | Review and improve monitoring/observability/logs setup (Netdata, Beszel, centralized logging) | User | 2026-01-18 | open | monitoring/, networking/

---

## Session 2026-02-14: Documentation & Tailscale Architecture

65 | tailscale_caddy_research | Research Caddy-based approach for Tailscale (user watched videos recommending this). Add as Option E to TAILSCALE_HOST_MODE_ALTERNATIVES.md | Claude | 2026-02-14 | open | truenas/TAILSCALE_HOST_MODE_ALTERNATIVES.md

72 | test_vscode_chat_panel_mcp | Test if Claude Code VSCode chat panel can access VSCode MCP servers (Context7, Pylance, Upstash). Compare with Copilot Chat capabilities. | User | 2026-02-14 | in_progress | ~/.config/Code/User/extensions/anthropic.claude-code-*

79 | health_check_audit | Audit all compose files across stacks for broken health checks. Known issues: jellystat used curl (not installed), fixed to wget. Pattern: verify the tool used in healthcheck EXISTS in the container image before deploying. | Claude | 2026-02-18 | open | truenas/stacks/*/compose.yaml

84 | mangarr_fork | Build a Radarr-based fork for manga/manhwa/webtoon acquisition. Radarr chosen as base (C#/.NET, open source, manga volumes are discrete releases like movies). Key changes: swap TheMovieDB metadata for MangaDex API, update search/matching for manga volume/chapter naming, swap quality profiles for CBZ/CBR/PDF formats, update file naming conventions. Prowlarr integration, download clients (qBittorrent/SABnzbd), UI, scheduling, notifications all reused unchanged. Prowlarr already supports manga indexers (NyaaTorrents, BakaBT etc). Intended to be open sourced — fills a real gap (Readarr retired 2026, Mangarr archived, Mylar3 has no manga support, Kapowarr rejected Prowlarr). MangaDex API: api.mangadex.org (free, no auth for basic queries). Radarr source: github.com/Radarr/Radarr | Claude | 2026-05-29 | open |

83 | watch_order_playlist_script | Build a script: given a show name, use AniDB HTTP API to get watch order (relations: sequel/prequel/movie), check Jellystat history for what's already watched, then create/update a Jellyfin playlist with remaining unwatched content in correct order. AniDB chosen over AniList — more authoritative data, already integrated in the stack. Manual process documented in ai/SESSION_NOTES.md → "Watch Order Playlists". APIs verified: Jellystat GET /api/getHistory, Jellyfin POST /Playlists. AniDB HTTP API: http://api.anidb.net:9001/httpapi — rate limit 1 req/2s, register client at anidb.net. | Claude | 2026-05-29 | open | truenas/scripts/, ai/SESSION_NOTES.md

81 | subdl_re_enable | Re-enable subdl provider in Bazarr once the KeyError: 'subtitles' bug is fixed upstream. Bug: `result['subtitles']` is accessed without checking key existence in `custom_libs/subliminal_patch/providers/subdl.py` — API sometimes returns success without that key. Fix is `result.get('subtitles', [])`. Track: https://github.com/morpheus65535/bazarr — check releases for subdl fix. Config: `media/.config/bazarr/config.yaml` → `enabled_providers`. | Claude | 2026-03-31 | open | media/.config/bazarr/config.yaml

87 | mempalace_value_review | Evaluate whether MemPalace is delivering value. It saves diary + drawers at session end and now loads via SessionStart hook — but if context injected is noisy/irrelevant or token cost exceeds benefit, remove it. Review after ~4 weeks of use (target: 2026-07-01). Signals to check: does wake-up output actually inform responses? Does it surface past decisions unprompted? If not — remove plugin, Stop/PreCompact hooks, and SessionStart hook. | Claude | 2026-06-01 | open |

86 | recurring_workflow_skills | Identify repeating homelab workflows that would benefit from being Claude skills with explicit model routing (e.g. Haiku for log triage → Sonnet for fix → Haiku for summary). Candidate areas: TrueNAS service health checks, Infisical secret rotation, arr-stack health + missing media triage, Caddy config changes. Goal: spawn subagents at appropriate model tiers instead of running everything in main thread. Use write-a-skill skill to build them. | Claude | 2026-06-01 | open | .claude/skills/, ai/PATTERNS.md


84 | autobrr_seed_script_test | Re-run autobrr seed_config.py on a clean instance to verify idempotency. Current live config was set up manually then script updated — needs end-to-end test. | Claude | 2026-05-22 | open | truenas/stacks/autobrr/seed_config.py

85 | bazarr_settings_api_bug | Bazarr POST /system/settings API silently ignores enabled_providers changes — only config.yaml edit works. If Bazarr is ever redeployed, re-add subf2m to enabled_providers in config.yaml before starting. | Claude | 2026-05-22 | open | /mnt/Fast/docker/bazarr/config/config.yaml

86 | animetosho_replacement | AnimeTosho is shutting down — replace it in Prowlarr. An NZB group is reportedly archiving all AnimeTosho content; find and add that indexer. Nyaa.si may cover the torrent gap. **Re-checked 2026-08-29**: AnimeTosho is still online and enabled in Prowlarr (indexer id 3, HTTP 200) — the shutdown hasn't happened 2.5 months after this item was raised. No action needed yet; re-check again if/when it actually goes down. | Kieran | 2026-06-13 | open (narrowed 2026-08-29 — not shut down yet) | truenas/stacks/prowlarr/

88 | autobrr_version_upgrade_test | autobrr is pinned to v1.79.0 (compose.yaml) because v1.80.0 crashes on startup with a nil pointer dereference in indexer.(*service).Start — restart loop, found empirically, no matching public GitHub issue found on search. Releases since then: v1.81.0, v1.82.0, v1.82.1, v1.83.0 (as of 2026-07-27), and now v1.84.0 (2026-08-13, confirmed via GitHub releases 2026-08-15) — none of their changelogs mention a fix for this specific crash, so it's unconfirmed whether it's resolved. As a future test, bump the pin to latest and watch `docker logs autobrr` closely right after `docker compose up -d --force-recreate` for a restart loop; revert to v1.79.0 immediately if it recurs. | Kieran | 2026-08-06 | open | truenas/stacks/autobrr/compose.yaml

90 | fileflows_version_upgrade_test | fileflows is pinned to 26.04 (compose.yaml) — a prior upgrade attempt past this version caused issues (per user, exact symptom not re-derived this session). Current upstream is 26.07 (confirmed via fileflows.com/docs/versions, 2026-08-15) — 3 releases behind. **This is the user's main video-editing asset pipeline — do not attempt an upgrade test without explicit go-ahead and a maintenance window**, unlike autobrr this isn't safe to just try-and-revert since a broken upgrade could disrupt active work, not just a restart loop. | Kieran | 2026-08-15 | open | truenas/stacks/fileflows/compose.yaml

89 | investigate_missing_episodes | Superseded/synced against media/docs/SONARR_STRUCTURAL_AUDIT.md, the authoritative live record — read that doc in full before touching this item again. **Re-verified live 2026-08-29 (see doc's "escape hatch" table updates + new root-cause section)**: The Big O now 26/26 (resolved). Macross II numbered season 6/6 (resolved); a separate, previously-untracked Season 0 "Macross II: The Movie" gap found, not yet searched. Tekkaman Blade "Virgin Memory" + all 4 OVA movies confirmed hasFile:true (resolved, see #97). Macross Delta's 2 specials confirmed hasFile:true (resolved). Robotech "Shadow Chronicles" confirmed hasFile:true (resolved); The Movie/Sentinels/Love Live Alive/Codename still missing, no new leads. AoT "Wings of Freedom" and JJK "Execution" both now `monitored:false` — no longer active gaps, dropped from this list. `.hack//Roots`/`.hack//Legend of the Twilight` — **correction 2026-08-29**: these are Season 3 and Season 2 of the combined `.hack` series (id 55), not standalone series — the 2026-08-24 session's "Manual Import mismatch" framing and series ids 145/146 were wrong; auto-match to series 55 was correct all along, content is already imported. **Genuinely still open, with real candidates found this pass (not yet grabbed)**: Gundam Wing "Endless Waltz: The Movie" (AnimeTosho now has 3 candidates), Gundam 0083 Season 0's "The Mayfly of Space 1 & 2" (episode ids 5446/5447 — correction 2026-08-29: earlier note conflated this with "Afterglow of Zeon" ep 5445, which itself has zero usable candidates, all `Unknown Series`) — **FULLY RESOLVED 2026-08-29**. Full incident, in order: (1) grabbed via Sonarr's `/api/v3/release` override — first attempt used a 0-scoring 480p AnimeTosho pack, hardcoded rather than checked against other candidates; (2) corrected to the actual best candidate (`[hchcsen]` S01 BD Remux, Nyaa.si, customFormatScore 1300) — but its override `episodeIds` were set to the pack's 13 numbered Season 1 episodes (already `hasFile:true`, ~100GiB pure duplicate) instead of the 2 actually-missing specials, which weren't included at all; user caught this and set the 13 duplicate files to "Do Not Download" in qBittorrent, leaving only the real `Mayfly of Space` bonus subfolder downloading; (3) once downloaded, `import_downloads.sh --scan-folder` hit a pre-existing script bug (unencoded folder path with spaces/brackets broke curl, exit 3) — fixed with `jq '$s|@uri'` URL-encoding; (4) folder-scanned as "Unknown Series" (expected, no seriesId hint per script's own documented rule), manually mapped `seriesId:133`/`episodeIds:[5446]`/`[5447]` per file from the scan's own quality/language data, submitted via `ManualImport` command — confirmed `"Manually imported 2 files"` and both episodes `hasFile:true`. **Lessons applied**: PATTERNS.md's override checklist (score-sort, hasFile-check before setting episodeIds, verify real file contents not just rejection text) and the URL-encoding fix in `import_downloads.sh` itself — see `media/docs/SONARR_ACQUISITION_PROCESS.md` step 6a-iii for the full writeup. **Still open, no new leads**: MF GHOST Battle Digest, Macross FB7 "Listen to My Song" (may be resolved per #109 — not re-checked), Gurren Lagann Parallel Works (series 147 was deleted entirely per user decision, doc says not wanted — drop from this list), Broken Blade (see #100). | Claude | 2026-08-07 | open (narrowed 2026-08-29) | media/docs/SONARR_STRUCTURAL_AUDIT.md

93 | disk_cleanup_remaining | Re-verified 2026-08-24 against live TrueNAS state (superseded by #92's dataset migration — old `/mnt/Data/media` and `/mnt/Data/downloads` are no longer the mounted paths for any *arr app, but leftover content wasn't cleaned up as part of that migration). (1) /mnt/Data/media/downloads/ — RESOLVED, path no longer exists (confirmed gone 2026-08-24), presumably cleaned up as part of the #92 migration. (2) /mnt/Data/media/.recycle — STILL OPEN, confirmed still 625GB as of 2026-08-24, user still hasn't decided whether to empty (forfeits recovery of recently-replaced files). (3) /mnt/Data/media/transcode_test — STILL OPEN, confirmed still 40GB as of 2026-08-24, looks like dead test data, not yet confirmed/deleted. (4) "partial match"/"no match" qbittorrent/completed items — NOT RE-VERIFIED this session, status unknown, re-check before assuming still relevant given the dataset migration likely moved/consolidated the downloads folder contents. | Kieran | 2026-08-18 | open (narrowed 2026-08-24 — only .recycle + transcode_test + item 4 remain) | N/A |
100 | broken_blade_no_clean_release | Broken Blade (Radarr 185-190) has no clean release available via Radarr's own search — only candidate it finds (AnimeRG) is both auto-rejected (BR-DISK/No-RlsGroup, score -20000) and on the Anime LQ Groups blacklist; confirmed 2026-08-29 that Radarr's own interactive search (`GET /api/v3/release?movieId=185`) doesn't surface the batch release at all, only an unrelated single-movie candidate — genuine title-matching miss, not a scoring issue. **Grabbed 2026-08-29 via the Prowlarr escape hatch** (`[Lulu] Break Blade (Broken Blade) Movies [BD 1080p Hi10 FLAC][Dual-Audio]`, Nyaa.si, 25 seeders) but **user caught and removed it — actually scores -200000** (a different, worse custom-format penalty than the already-known AnimeRG rejection; specific CF not yet identified — check per-movie `POST /api/v3/release` scoring output next time before grabbing, not just seeder count/blacklist membership). Still genuinely no clean release found. `[Exiled-Destiny] Broken Blade Movie 1-6 [Dual Audio]` (18 seeders) noted earlier as an alternate candidate, not yet checked. | AI | 2026-08-22 | open (Lulu release rejected by user, still no clean source) | media/docs/SONARR_STRUCTURAL_AUDIT.md
103 | prowlarr_escape_hatch_downloadclient | Added qBittorrent + SABnzbd download clients to Prowlarr (ids 1/2, category `prowlarr-escape-hatch` on both) so manual Prowlarr grabs go through Prowlarr's own client integrations instead of hand-rolled magnet-add curl calls. **Both confirmed working end-to-end 2026-08-24.** qBittorrent path sidesteps qBittorrent v5.x bug (github.com/qbittorrent/qBittorrent issue #24367: `/api/v2/torrents/add` returns a success-shaped response but silently fails to add after extended container uptime, confirmed hit this session on 2 grabs) — grabbed `.hack//Legend of the Twilight` via `POST /api/v1/search {guid,indexerId}`, landed correctly. SABnzbd path initially 403'd (`External internet access denied`) despite identical host/port/urlBase/apikey config to Sonarr's already-working SABnzbd client (id 3) on the same subnet — **root cause found**: SABnzbd's `local_ranges` only listed IPv4 CIDRs, but Prowlarr's .NET HTTP client (unlike Sonarr's, for reasons internal to each app's networking code — ruled out OS/libc, all three Servarr containers are identical Alpine base) prefers the container's real IPv6 address on `arr-stack_default` (confirmed via `docker network inspect`: every container on that network has both an IPv4 and an IPv6 ULA address, e.g. Prowlarr `fdc3:63f0:a0c0:9::a`). Fixed by adding `fdc3:63f0:a0c0:9::/64` to SABnzbd's `local_ranges` in `sabnzbd.ini` (API silently ignores list-type config writes, same known issue as Bazarr's `enabled_providers` — had to edit the file directly) plus adding a matching `prowlarr-escape-hatch` SABnzbd category (also ini-only, no API support for creating categories). Restarted via **Dockhand's API** (`POST /api/containers/{id}/restart?env=1`), not SSH+docker compose — `downloaders` is a Dockhand git-sourced stack (see COMPLETED.md #77), and CLAUDE.md's "TrueNAS App Management" section listing it under midclt-managed native apps is stale, needs correcting. **The broader question — why every docker network on this host has IPv6 enabled by default (`/etc/docker/daemon.json`: `"ipv6": true`, TrueNAS-level default, not from our compose files) — was deliberately NOT touched**, narrow per-app fix only; disabling IPv6 daemon-wide would recreate every network on the host and needs its own deliberate session, tracked separately below. **Still needed**: a Cleanuparr seeding rule for the `prowlarr-escape-hatch` category (mirroring tv-sonarr/radarr, same `download_client_config_id` 196CBAFB-5D45-416E-95E6-D63CD46F32BC) so escape-hatch torrents get cleaned up on schedule — requires Cleanuparr's web UI (JWT auth) or a raw SQLite INSERT into its live DB, neither attempted. | AI | 2026-08-24 | mostly done 2026-08-24 (Cleanuparr rule still open) | truenas/stacks/prowlarr/, ai/PATTERNS.md, CLAUDE.md
104 | docker_ipv6_daemon_wide_review | Every Docker network on TrueNAS has IPv6 enabled by default (`/etc/docker/daemon.json`: `"ipv6": true`, `"default-network-opts":{"bridge":{"com.docker.network.enable_ipv6":"true"}}`, `fixed-cidr-v6: fdd0::/64`) — a TrueNAS-level default, confirmed present on all ~15 app networks (arr-stack, downloaders, jellyfin, autobrr, commafeed, homepage, etc.), not something set by our own compose files. User confirmed IPv6 isn't actually used/wanted. This caused a real bug (see #103 — Prowlarr↔SABnzbd 403, root-caused to Prowlarr preferring the container's real IPv6 address). Worth a deliberate review: whether to disable IPv6 daemon-wide (likely via TrueNAS System Settings → Apps → Docker config, or `midclt call docker.update` — NOT a raw daemon.json hand-edit, TrueNAS's middleware manages that file) — this recreates every container network on the host, so needs a maintenance-window mindset, not a casual change. | Kieran | 2026-08-24 | open | N/A
105 | reduce_command_boilerplate | Session commands are needlessly large/repetitive — nearly every Bash call this session re-declared the same `INFISICAL_PROJECT_ID`/`_isec()` secret-fetch boilerplate and re-authenticated to qBittorrent/Dockhand/Prowlarr from scratch, even across calls seconds apart. Build tooling to cut this down: (1) a sourceable shell helper (e.g. `truenas/scripts/lib.sh`) providing `_isec()` and pre-fetched common API keys as env vars for a session, so individual commands are just the actual API call; (2) consider a short-lived local cache for auth cookies (qBittorrent, Dockhand) instead of re-logging-in every call — cookies are already time-limited so this is low-risk; (3) review whether small wrapper CLI scripts (e.g. `sonarr-api <path>`, `qbit-api <method> <path>`) are worth the maintenance cost vs. just keeping the helper-sourcing pattern. Should not compromise the no-secrets-in-output rule — helper must still avoid printing keys. | Kieran | 2026-08-24 | open | ai/PATTERNS.md, truenas/scripts/
106 | season0_sweep_stale_backlog | **Resolved 2026-08-24.** Full live re-pull done (75 series w/ Season 0 content, ~700+ unmonitored items). ~650 confirmed bulk promo/recap/BTS junk, bulk-marked ⚪ per user approval. Genuine-content franchises worked one at a time with per-franchise check-ins: Robotech, VOTOMS, Steins;Gate, Steins;Gate 0, Macross Delta, Mazinkaiser, Full Metal Panic!, Mushoku Tensei kept/grabbed; JUJUTSU KAISEN, Darker Than Black, JoJo's Bizarre Adventure (2012) grabbed then reverted per user decision (not committed to continuing those shows — see new "not committed" monitoring-pattern rule in the doc); Golden Kamuy skipped by that same rule before even checking its 4 real-looking titles. Full detail in media/docs/SONARR_STRUCTURAL_AUDIT.md's new "2026-08-24 Season 0 sweep" section. | AI | 2026-08-24 | closed | media/docs/SONARR_STRUCTURAL_AUDIT.md
107 | season0_sweep_followups | **Closed 2026-08-29.** Follow-ups from the 2026-08-24 Season 0 sweep, all resolved this session (see media/docs/SONARR_STRUCTURAL_AUDIT.md's Robotech entry for full detail): Robotech "Shadow Chronicles" re-grabbed + imported (prior grab had gone stale/dropped, root cause tracked separately as #112). Macross Delta "Passionate Walküre" + "Zettai Live!!!!!!" re-grabbed + imported. Steins;Gate "Egoistic Poriomania" re-grabbed (original 1-seeder release found dead, then a 66MB "complete" torrent found to be fake/broken — both replaced with a real NTb NZB release) + imported. Mazinkaiser confirmed already resolved 2026-08-15 (the "still stuck" note here was stale). "Codename: Robotech" corrected — confirmed via web research to be a 1985 promotional recap, not a real gap, unmonitored in Radarr. "The Movie"/"The Sentinels" confirmed genuinely unavailable via sourced research (real distinct films, not recaps) plus a full re-run escape hatch (including the "Untold Story" alternate title) — still zero results. "Love Live Alive" real candidates exist but all 0-seeder, not grabbed. Bonus: found and imported Robotech Season 1 + Season 3 BluRay/CR-WEBDL upgrades (61 episodes) via Treasure Maps season packs the user found directly. VOTOMS Vol.1/Vol.2, Steins;Gate 0 "Bittersweet Intermedio", Full Metal Panic! "Episode 000" — untouched this session, still open (not part of the Robotech-focused follow-up), see SONARR_STRUCTURAL_AUDIT.md if resuming. | AI | 2026-08-29 | closed | media/docs/SONARR_STRUCTURAL_AUDIT.md

108 | truenas_update_strategy | Decide how to handle TrueNAS Scale version updates (currently 25.10.1) — whether to update at all given the fully custom Dockhand-managed compose stack setup, how to test/stage before applying, backup/rollback plan for the boot pool, and whether ZFS ARC tunable / mem_limit caps (set 2026-08-24 after OOM/reboot incident) need re-verification post-update since some settings may reset. | Claude | 2026-08-24 | open | truenas/DOCKHAND_READINESS.md, truenas/DOCKHAND_GITOPS_GUIDE.md

109 | macross7_season0_reassessed | Macross 7's Season 0 assessed per the "series or movie" process (2026-08-25). **Dynamite 7**: had a real dedicated TVDB entry (442168) but split-out attempt hit an unresolved Jellyfin series-identification bug (see #110) — reverted, Sonarr entry + files deleted, back to unmonitored specials. **Encore (3 eps) + Plus (12 eps)**: no dedicated TVDB/TMDB entry exists for either — left as Macross 7 Season 0 specials, re-monitored since they're real content (14/15 already had files; "Plus 12 - Let's Bomber" grabbed via a low-seeder ~9-seed pack, stalled at 0%, check back). **FB7 "Listen to My Song!"**: confirmed a genuine standalone TMDB movie (id 258156) — turns out it was never actually downloaded anywhere despite earlier assumption otherwise; grabbed via Prowlarr into the existing (previously empty) Radarr entry (id 62), downloading as of session end. | AI | 2026-08-25 | mostly done (Plus 12 + FB7 still downloading) | media/scripts/watch_orders/macross.json

110 | jellyfin_series_identification_bug | Jellyfin failed to register a new TV series (tested with "Macross Dynamite 7", tvdbid 442168, folder correctly named `Macross Dynamite 7 (1997) [tvdbid-442168]`) as queryable via search, library browse, or the web UI — despite 2 library rescans, a full recursive `ReplaceAllMetadata=true` refresh, and a full container restart (via Dockhand). Container logs showed `SeriesMetadataService` actively running TVDB/TMDB/NFO providers and saving the item as a `TV.Series` entity on every attempt, but it never surfaced anywhere queryable. A likely-unrelated stray empty Radarr movie folder with a near-identical name (`/data/movies/Macross Dynamite 7 (1997) [tmdbid-1373767]`, Radarr id 51) was found nearby but ruled out as the cause (no children, separate library). Root cause never found — abandoned rather than resolved; the series was deleted entirely rather than fixed. If a future Sonarr-side series split is attempted again, check the Jellyfin dashboard UI directly early rather than repeating a long API/log debugging cycle. | AI | 2026-08-25 | open | N/A
111 | caddy_loki_hash_needs_infisical_agent_templating | The new `loki.home` Caddy vhost (added 2026-08-29 for scripts to push logs to Loki without SSH) has its basic_auth bcrypt hash baked as a literal string directly in `truenas/stacks/caddy/Caddyfile` — generated once from `LOKI_PUSH_PASSWORD_HASH` in Infisical `/observability` via `htpasswd -bnBC 10 "" '<password>'` (not `caddy hash-password`, to avoid an ad-hoc `docker run` on the Dockhand-managed TrueNAS host). This means rotating `LOKI_PUSH_PASSWORD` requires manually regenerating the hash and hand-editing/redeploying the Caddyfile — it doesn't flow through automatically the way `arr-stack`/`downloaders`/`jellyfin`/`homepage`'s `.env` files do via the Infisical Agent. Proper fix: wire Caddy into the same Infisical Agent template-rendering pattern (render the Caddyfile, or at least the basic_auth line, from a template on each Infisical secret change) so the hash stays in sync automatically like every other secret in this repo. | AI | 2026-08-29 | open | truenas/stacks/caddy/Caddyfile, truenas/stacks/caddy/compose.yaml


---

## Migrated from ai/handoff-*.md (consolidated 2026-08-29, handoff files deleted)

113 | dockhand_auto_update_strategy | Decide how to auto-update Dockhand-managed container images (raised 2026-06-06, never decided). Options considered: Renovate Bot (PRs image bumps, Dockhand deploys on merge — best GitOps fit), Watchtower (polls + auto-restarts, simpler but unreviewed), or Dockhand self-managing its own version bump in git post-cutover. | Claude | 2026-06-06 | open | truenas/DOCKHAND_GITOPS_GUIDE.md

114 | observability_signoz_trial_cleanup | SigNoz (observability trial 1) is built and stopped but not deleted — Dockhand git-stack id 16, `autoUpdate:false`, config/data preserved at `/mnt/Fast/docker/signoz/`. Grafana Alloy+Loki+Prometheus (trial 2) won and is now the active stack in production use (per later commits rotating its Loki push credentials). Decide whether to fully delete the SigNoz stack/data or keep it stopped indefinitely as a fallback. | Claude | 2026-08-22 | open | truenas/stacks/signoz/

115 | grafana_dashboard_tooltip_single_mode | User explicitly asked to set tooltip mode to "Single" (not "All") on the imported "Docker monitoring with service selection" Grafana dashboard (UID `m0arCBf7k`) to work around an unfixable upstream Grafana bug (tooltip overflow with many series, grafana/grafana#46131). Needs `options.tooltip.mode` set on each multi-series panel (CPU, Memory, Network, Disk I/O) via dashboard JSON or UI, then re-saved as the default. Not done as of 2026-08-22. | Kieran | 2026-08-22 | open | grafana (dashboard UID m0arCBf7k)

116 | shoko_anidb_import_progress | Shoko Server's AniDB library-match import was still running as of 2026-08-24 (rate-limited API, ~2/74+ series matched at session end). Check `GET /api/v3/Queue` and `GET /api/v3/Series?pageSize=0` on Shoko's API for current progress; Shokofin's watch-order playlist features in Jellyfin populate as more series get matched. | Claude | 2026-08-24 | open | truenas/stacks/shokoanime/

117 | jellyfin_anime_library_dedup | New Shoko-powered "Anime" Jellyfin library duplicates titles already present in the existing TV/Movies libraries (accepted deliberately by user for now). Revisit excluding anime folders from the original TV/Movies libraries' scope once satisfied with how the Shoko-powered library looks. | Claude | 2026-08-24 | open | jellyfin (library config)

118 | dual_sonarr_for_anime_deferred | "Sonarr acquires, Shoko organizes" hybrid was researched (needs Sonarr's Completed Download Handling disabled, which is instance-global not per-series, so would require a second dedicated Sonarr instance) and deliberately deferred, not started. Only relevant if Shoko's own renamer (currently off) is turned on to actually organize/rename files. | Claude | 2026-08-24 | open | truenas/stacks/shokoanime/, truenas/stacks/arr-stack/

119 | dockhand_infisical_secrets_provider_adoption | Dockhand's native Infisical secrets-provider integration was proven working (`POST /api/secret-providers`, registered as provider id 1 `infisical-homelab`, tested `ok:true` against the same dev/`/media` path) but not bound to any stack yet. Could eventually replace `infisical-agent` + `.tmpl` files entirely for every stack via `PUT /api/git/stacks/{id}` with `secretProviderId`. Bigger architectural change — treat as a deliberate follow-up, not ad hoc. | Claude | 2026-08-24 | open | truenas/stacks/infisical-agent/

120 | fleet_memory_caps_remaining | Only `shoko_server` (2g) and `qbittorrent` (4g) got `mem_limit` caps based on real 2-day Prometheus peak data (2026-08-24, after an OOM host crash). Rest of the fleet (jellyfin/caddy/infisical/etc.) was already capped pre-incident and showed peaks well under 1GiB — low priority, but could get modest default caps later as cheap insurance. Query Alloy/Prometheus (`http://127.0.0.1:9090` on TrueNAS) for real peak data before setting any cap, don't guess. | Claude | 2026-08-24 | open | truenas/stacks/*/compose.yaml

121 | sonarr_audit_doc_further_trim | `media/docs/SONARR_STRUCTURAL_AUDIT.md` was reorganized 2026-08-29 (Consolidated index / Reference tables / Investigation log / regenerate-data, verbatim line-slice, no content merged) — user thinks it can be trimmed further (e.g. folding Investigation log facts into the index rows so the index is fully self-contained, cutting genuinely stale/superseded narrative). Explicitly deferred, not urgent — revisit next session. | Claude | 2026-08-29 | open | media/docs/SONARR_STRUCTURAL_AUDIT.md
