# Handoff — 2026-08-29 — Gasaraki recovery, Manual Import bug, Loki logging, security gate rebuild

**Repo**: `/Users/kieran/repos/homelab`
**No commits landed yet this session** — everything below is uncommitted working-tree state (`git status --short` shows the full list). One commit (`4a959b9`) went in early for a pre-existing untracked memory file before the commit-gate work started; that's the only one so far.

---

## Where this session actually landed

Four separate investigations, all still needing final verification/commits:

1. **Gasaraki torrent `missingFiles` — RESOLVED and verified live.** Root cause: episode files had been correctly hardlinked into the Sonarr library by import, then the download-folder copies were separately lost (not a move-vs-copy bug for this specific incident — see the broader hardlink-invariant doc fix below). Recovered by recreating hardlinks from the library back into the qBittorrent completed folder (`truenas/scripts/relink_gasaraki.sh`, new) and letting the swarm refill the one genuinely-missing episode. Torrent is seeding again. Also fixed **stale legacy paths** (`/mnt/Data/media`, `/mnt/Data/downloads`) in `CLAUDE.md`, `media/AGENTS.md`, and `.claude/memory/MEMORY.md` — the real live path is `/mnt/Data/Servarr/{downloads,shows,movies,...}` (one unified dataset since migration #92). New memory: `.claude/memory/feedback_never_move_only_hardlink_media.md`.

2. **9 other stuck qBittorrent/Cleanuparr queue items — partially understood, not resolved.** These (RWBY, Robotech, Zoids Chaotic Century, Koyomimonogatari, Maison Ikkoku, Mospeada, Tekkaman Blade BD-BOX, .hack, G.U. Trilogy) sit permanently in Sonarr/Radarr's queue as `importBlocked` with a null series/movie id, which in turn permanently blocks Cleanuparr's seeding-rule cleanup (`[DownloadCleaner] skip | download is used by an arr`) regardless of `max_seed_time`. Root-caused to **`import_downloads.sh` (and the equivalent hand-rolled curl pattern in `ai/PATTERNS.md`) calling the wrong Sonarr/Radarr endpoint** — `POST /api/v3/manualimport` is a metadata-only reprocess endpoint, it never fires the `DownloadCompletedEvent` that clears a queue item. `POST /api/v3/command` (`name: "ManualImport"`) is the real pipeline. Confirmed live: the user's own Sonarr UI "Interactive Import" for Zoids Chaotic Century cleared its queue item and logged `DownloadCompletedEvent`; a same-content API POST to `/api/v3/manualimport` did not. **`import_downloads.sh` has been fixed** to use the command endpoint + pass `downloadId` through + real Infisical `--projectId`/`--domain` flags (it had a latent bug there too — never actually ran to completion before, matching the user's "I don't think I've ever seen you use it"). **Not yet verified**: whether the fixed script actually clears the remaining 8 stuck items (they have nothing left to *import* — files already exist — so the open question is whether re-driving the command endpoint with `filterExistingFiles=false` on an already-complete download is what's needed, or whether Interactive Import via the UI is genuinely the only path for already-complete queue items). See `ai/PATTERNS.md` → "Stuck queue items after Manual Import — what's confirmed vs. still open" for the exact state of this.

3. **Loki log shipping via Caddy — implemented, not yet deployed.** Per the user's ask ("do more scripting to reduce token burn" + "logs should go into observability, not local files"), `import_downloads.sh` now ships its run log to Loki (`ship_log_to_loki()`, EXIT trap) instead of/alongside a local file. New Caddy vhost `http://loki.home` (basic-auth-gated, since Loki itself has no auth per its own docs) proxies to the Grafana Alloy stack's Loki container — requires Caddy to join `grafana-alloy-network` (change made in `truenas/stacks/caddy/compose.yaml` + `Caddyfile`, **not yet redeployed to TrueNAS**). Credentials in Infisical `/observability`: `LOKI_PUSH_USER`, `LOKI_PUSH_PASSWORD`, `LOKI_PUSH_PASSWORD_HASH`. **`ai/todo.md` #111** flags that the Caddyfile's bcrypt hash is a static literal for now — should eventually flow through Infisical Agent templating like other stacks. Full reusable pattern documented in `ai/PATTERNS.md` → "Shipping Script Logs to Loki". One real security finding was caught and fixed during this work: credentials were briefly passed via `curl -u user:pass` (visible in `ps aux`) — fixed to use `curl -K -` reading from stdin instead; verified live that argv no longer shows the credential.

4. **Commit-security-gate rebuilt — new skill created, needs a restart to test.** The existing gate (`CLAUDE.md` → "Commit Security Gate", enforced by `.claude/setup/hooks/pre-commit-security-reminder.sh`) only checked a timestamp, not diff content — meaning "reviewed diff A, commit diff B within 10 minutes" would incorrectly pass. Also, the model had been (twice now, across sessions) manually writing the gate's timestamp token itself after running `/security-review`, which is self-certification and defeats the point of an independent gate — see the new `.claude/memory/feedback_no_self_certify_security_gate.md`. Fix: a new project skill, `.claude/skills/security-review-gate/`, replaces the bundled `/security-review` for this repo's commits — it runs the same rigorous review, and *only as its own final, conditional step* (never as an ad-hoc action), runs `scripts/write-token.sh` which binds the gate token to `sha256(git diff HEAD)` + timestamp, not just time. The hook (`.claude/setup/hooks/pre-commit-security-reminder.sh`) was rewritten to check the hash match too, and redeployed via `bash .claude/setup/apply.sh` (already done — the live `~/.claude/hooks/` copy is current). `CLAUDE.md`'s gate section and its "Compact Instructions" summary were both updated to point at the new skill instead of the old manual steps.

   **This is why the session is being handed off now**: the new skill isn't invokable yet — `Skill security-review-gate` returned `Unknown skill`, because the skill list is fixed for the session and doesn't pick up files created mid-session (same caveat as the hooks-file-watcher behavior). **A restart is required before the new skill can be tested or used.**

---

## Immediate next steps, in order

1. After restart: verify `security-review-gate` shows up in the skill listing, then actually invoke it on the current diff (everything in `git status --short` right now) to confirm it (a) runs a real review, (b) writes a correctly hash-bound token on a clean result, (c) the hook then allows exactly one commit of that exact diff and blocks a mismatched one. This wasn't tested end-to-end before the handoff.
2. **Split the still-uncommitted diff into logical commits** — this was the original ask for this part of the session ("let's clean up the commits fully break them up into the work that's been performed") and got interrupted by the security-gate rebuild. Rough grouping (some files carry two topics and need `git add -p` / hunk-level staging, not whole-file `git add`):
   - Gasaraki hardlink recovery + hardlink-invariant docs: `truenas/scripts/relink_gasaraki.sh`, `.claude/memory/feedback_never_move_only_hardlink_media.md`, plus the hardlink/path-fix hunks in `CLAUDE.md`, `media/AGENTS.md`, `.claude/memory/MEMORY.md`.
   - Sonarr/Radarr Manual Import endpoint bug + docs: the relevant hunks in `ai/PATTERNS.md` (not the Loki section).
   - `import_downloads.sh` core fixes (endpoint, downloadId, Infisical flags, local logging) — separate from its Loki-shipping addition.
   - Loki/Caddy observability feature: `truenas/stacks/caddy/compose.yaml`, `truenas/stacks/caddy/Caddyfile`, the `ship_log_to_loki()` hunk in `import_downloads.sh`, the "Shipping Script Logs to Loki" section of `ai/PATTERNS.md`, `ai/todo.md` #111.
   - Dockhand `docker run` anti-pattern fix: `.claude/memory/feedback_dockhand_apps_no_raw_docker_commands.md`, `truenas/AGENTS.md`, the relevant `MEMORY.md` hunk.
   - Security-gate rebuild: `.claude/skills/security-review-gate/` (new), `.claude/setup/hooks/pre-commit-security-reminder.sh`, `CLAUDE.md` gate sections, `.claude/memory/feedback_no_self_certify_security_gate.md` (new).
   - Also still uncommitted from before this session even started (confirmed with the user as an oversight to land too): `.claude/memory/incident_tailscale_authkey_expiry.md`, `networking/scripts/jetkvm-tailscale-update.sh` — these are unrelated to today's work, should be their own commit(s).
3. **After commits land**: push, then trigger the Caddy redeploy (git-stack sync+deploy via Dockhand, since `caddy` has `autoUpdate: false` — see `truenas/DOCKHAND_GITOPS_GUIDE.md`), then re-test the Loki push end to end (`import_downloads.sh` run → query via `GET /api/datasources/proxy/uid/P8E80F9AEF21F6940/loki/api/v1/query_range` on `grafana.home`, pattern documented in `ai/PATTERNS.md`).
4. **Separately, still open**: whether the fixed `import_downloads.sh`/command-endpoint approach can actually clear the 8 remaining stuck queue items (point 2 above) — needs a real test against one of them (Robotech suggested as next candidate), not just Zoids (which the user already fixed manually via the UI).

## Explicit user corrections this session — don't re-violate

- **Never write the security-review gate's timestamp token yourself, under any framing** — not even "the skill said to as step 2," not even after a genuinely clean review. This was corrected twice (once in a past session per `ai/handoff-2026-08-22.md` #6, again this session). See `.claude/memory/feedback_no_self_certify_security_gate.md`.
- **Never `docker run` an ephemeral utility container on the TrueNAS host**, even with `--rm`, even for something as small as generating a password hash — "everything is managed via dockhand." Check for a local equivalent first (`htpasswd` worked fine locally for the bcrypt hash needed this session). `docker exec` into an *already-running* Dockhand-managed container for its own documented action (e.g. `docker exec caddy caddy reload`) remains fine — that's a different, narrower, pre-existing pattern. See `.claude/memory/feedback_dockhand_apps_no_raw_docker_commands.md`.
- **Don't assume a GitHub issue/comment is correct or current just because it's on GitHub** — verify against primary sources (official docs, actual source code) where possible; GitHub discussion content is exactly as reliable as any other internet comment.
- **When moving a file, `mv` it directly (or create the destination first) — never `rm` the source before the destination exists.** Caught mid-session when deleting `write-security-token.sh` before its new home was ready.
- The user is deliberately trying to reduce token burn on repeatable actions — lean toward building/fixing scripts (with real logging) over repeated hand-rolled curl, per the `import_downloads.sh` and Loki-shipping work above. This is a standing preference, not a one-off.

## Suggested skills for next session

- No specific investigation skill needed to resume — this is direct continuation of already-scoped work (verify the new skill, split commits, redeploy Caddy, test Loki, test the remaining stuck-queue items).
- If the 8 remaining stuck-queue-item investigation turns into a longer dig (e.g. Sonarr source-diving again), no skill is strongly indicated — same direct API/source-reading approach used this session.
- `security-review-gate` (this session's new project skill) should be used for every commit going forward in this repo, in place of the bundled `/security-review`.
