# Sample environment variables for Homepage service
# The compose already reads ../../media/.env; prefer to set real API keys there.
# Copy relevant API keys from your service configs into ../../media/.env

# Media service API keys (fill these if not present in ../../media/.env)
RADARR_API_KEY=
SONARR_API_KEY=
PROWLARR_API_KEY=
BAZARR_API_KEY=
JELLYFIN_API_KEY=

# Download clients
QBITTORRENT_USER=
QBITTORRENT_PASS=

# Example: to use media/.env values, do not duplicate here — this file only documents keys.
HOMEPAGE_IMAGE=gethomepage/homepage:latest
HOMEPAGE_PORT=3000
SERVARR_NETWORK_NAME=servarrnetwork
